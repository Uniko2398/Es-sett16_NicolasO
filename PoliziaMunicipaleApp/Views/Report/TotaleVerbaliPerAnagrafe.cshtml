@model IEnumerable<dynamic>

<h2>Totale Verbali per Anagrafe</h2>
<table class="table">
    <thead>
        <tr>
            <th>Trasgressore</th>
            <th>Totale Verbali</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Anagrafica</td>
                <td>@item.TotaleVerbali</td>
            </tr>
        }
    </tbody>
</table>